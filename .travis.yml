language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly

after_success:
  - if [ "$TRAVIS_RUST_VERSION" != "1.2.0" ]; then
      cargo coveralls --verbose;
    fi
